name: Deploy & Test Elasticsearch

on:
  push:
    branches:
      - 'release/*'
    tags:
      - 'v*'
  workflow_dispatch:

env:
  RELEASE: elasticsearch-k8s
  NAMESPACE: elasticsearch

jobs:
  deploy-and-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up kubectl & helm
        run: |
          curl -fsSL https://raw.githubusercontent.com/helm/helm/main/scripts/get-helm-3 | bash
          curl -fsSL https://dl.k8s.io/release/v1.30.0/bin/linux/amd64/kubectl -o /usr/local/bin/kubectl
          chmod +x /usr/local/bin/kubectl

      - name: Configure kubeconfig
        run: |
          mkdir -p ~/.kube
          echo "${{ secrets.KUBECONFIG }}" | base64 -d > ~/.kube/config
          chmod 600 ~/.kube/config
          kubectl get namespace elasticsearch 2>/dev/null || echo "First deploy"

      - name: Deploy
        run: |
          helm repo add elastic https://helm.elastic.co
          helm repo update
          helm dependency update
          helm upgrade --install $RELEASE . \
            -n $NAMESPACE --create-namespace \
            --set domain=${{ vars.DOMAIN }} \
            --set email=${{ vars.EMAIL }} \
            --set storageProvisioner=${{ vars.STORAGE_PROVISIONER }} \
            --timeout 5m

      - name: Wait for pods
        run: |
          sleep 10
          kubectl rollout status statefulset/elasticsearch-master \
            -n $NAMESPACE --timeout=300s

      - name: Run tests
        env:
          DOMAIN: ${{ vars.DOMAIN }}
        run: bash tests/test.sh
