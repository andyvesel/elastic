apiVersion: v1
kind: LimitRange
metadata:
  name: elasticsearch-limits
  namespace: elasticsearch
spec:
  limits:
    - type: Container
      default:
        cpu: "2"
        memory: 2Gi
      defaultRequest:
        cpu: 500m
        memory: 2Gi
      max:
        cpu: "4"
        memory: 4Gi
